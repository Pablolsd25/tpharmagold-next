export default function Loading() {
  return (
    <div className="min-h-[60vh] bg-black flex items-center justify-center">
      <p className="text-accent font-display text-xs uppercase tracking-[0.35em] animate-pulse">
        Cargando…
      </p>
    </div>
  )
}
